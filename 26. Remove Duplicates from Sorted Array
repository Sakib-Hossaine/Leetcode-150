class Solution {
public:
    int removeDuplicates(vector<int>& nums) {
        set<int>un(nums.begin(), nums.end());
        int i=0;
         for(int num : un)
           {
            nums[i++]=num;
            //i++;
           }
       return un.size(); 
    }
};
